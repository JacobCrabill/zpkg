# Test Fixture Conventions

This tree reserves stable homes for future golden and integration coverage.

- `golden/` contains expected normalized outputs checked into source control.
- `integration/` contains fixture inputs and scratch layout conventions for end-to-end tests.

Phase 00 establishes the directory structure only; later lanes can add concrete fixtures and tests without reshaping this tree.
