# Integration Fixtures

Use this tree for end-to-end and workflow-oriented fixtures.

Reserved subdirectories:
- `fixtures/` for checked-in input package graphs and malformed cases
- `store/` for fake artifact-store layouts used by tests
- `workspaces/` for expected realized-workspace layouts or scratch conventions
